import { ImagenFondoDirective } from './imagen-fondo.directive';

describe('ImagenFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenFondoDirective();
    expect(directive).toBeTruthy();
  });
});
